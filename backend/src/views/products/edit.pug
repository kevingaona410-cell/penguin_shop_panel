extends ../layout

block content
  section.page-heading
    .heading-copy
      p.eyebrow Catálogo
      h1 Editar producto
      p Actualiza la información del artículo seleccionado.

  - const formAction = `/admin/products/${product._id}?_method=PUT`
  - const submitLabel = 'Guardar cambios'
  include _form